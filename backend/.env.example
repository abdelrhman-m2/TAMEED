# Server
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:8080,http://localhost:5173

# PostgreSQL
DATABASE_URL=postgres://taamed:taamed@localhost:5432/taamed
# Or individual fields
PGHOST=localhost
PGPORT=5432
PGUSER=taamed
PGPASSWORD=taamed
PGDATABASE=taamed

# AI
GROQ_API_KEY=replace_with_real_key
GROQ_MODEL=llama-3.1-8b-instant

# Lead notifications
SALES_EMAIL=mekkawy@tameed.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=replace_with_password
SMTP_FROM="TAMEED <no-reply@tameed.com>"
